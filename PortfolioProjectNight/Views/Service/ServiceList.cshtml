@model List<PortfolioProjectNight.Models.Service>

@{
    ViewBag.Title = "ServiceList";
    Layout = "~/Views/Admin/Index.cshtml";
}

<h2 class="mb-4" style="text-align:center">Hizmetlerim</h2>

<table class="table table-bordered">
    <tr>
        <th>ID</th>
        <th>Başlık</th>
        <th>İkon</th>
        <th>Açıklama</th>
        <th>Sil</th>
        <th>Güncelle</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Serviceld</td>
            <td>@item.Title</td>
            <td><i class="@item.Icon"></i>  (@item.Icon) </td>
            <td>@item.Description</td>
            <td><a href="/Service/DeleteService/@item.Serviceld" class="btn btn-danger">Sil</a></td>
            <td><a href="/Service/UpdateService/@item.Serviceld" class="btn btn-warning">Güncelle</a></td>
        </tr>
    }
</table>
<a href="/Service/CreateService/" class="btn btn-dark">Yeni Hizmet Ekle</a>
